# MySQL connection — same values as your Laravel backend/.env
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=medical_doc_engine
DB_USERNAME=root
DB_PASSWORD=

# API key — share this with anyone calling the service
API_KEY=replace_with_a_strong_random_key
